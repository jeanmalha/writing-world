# Copy to infra/backend.env and fill in your values.
# backend.env is gitignored — never commit it.

AWS_PROFILE=perso

# Must be globally unique across all AWS accounts.
# e.g. writingworld-jm42  (use a short unique suffix)
COGNITO_DOMAIN_PREFIX=writingworld-yourname

# Optional — override the Bedrock model
# BEDROCK_MODEL=anthropic.claude-3-haiku-20240307-v1:0

# IMPORTANT: Before deploying, enable model access in the AWS Console:
# Amazon Bedrock → Model access → Request access to Anthropic Claude models
# (in the same region as this stack, us-east-1)
